[package]
name = "wiremux-build-helper"
version = "0.1.0"
edition = "2021"

[dependencies]
dialoguer = { version = "0.11", default-features = false }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
