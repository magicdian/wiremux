# Espressif Vendor Layout

This directory contains Espressif-facing Wiremux integration sources.

- `generic/` holds the current shared ESP-IDF component and example.
- `s3/` is reserved for ESP32-S3 specific overrides.
- `p4/` is reserved for ESP32-P4 specific overrides.
